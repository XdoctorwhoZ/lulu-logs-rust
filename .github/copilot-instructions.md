# Copilot Instructions — lulu-logs-rust

## After every change

Always run the full test suite before considering any task complete:

```bash
cargo test
```

All tests must pass (0 failures) before finishing work.
